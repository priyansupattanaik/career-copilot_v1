"use client";
import{useState}from"react";import{useRouter}from"next/navigation";import{Button,Card,Input,PageHeader,Textarea}from"@/components/ui/primitives";import{apiRequest}from"@/lib/api/client";
export function Onboarding(){const router=useRouter();const[form,setForm]=useState({full_name:"",headline:"",phone:"",location:"",current_role:"",bio:""});const[error,setError]=useState("");async function save(){try{await apiRequest("/profile",{method:"PATCH",body:JSON.stringify(form)});router.replace("/dashboard")}catch(e){setError((e as Error).message)}}return <main id="main-content" className="section"><div className="container"><PageHeader eyebrow="Candidate onboarding" title="Build your persisted profile" description="These details are saved to your private Supabase profile."/><Card className="stack"><div className="grid-2">{([['full_name','Full name'],['headline','Professional headline'],['phone','Phone'],['location','Location'],['current_role','Current role']] as const).map(([key,label])=><label className="field-label" key={key}>{label}<Input value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></label>)}</div><label className="field-label">Bio<Textarea value={form.bio} onChange={e=>setForm({...form,bio:e.target.value})}/></label>{error&&<p className="field-error">{error}</p>}<Button onClick={save}>Save profile</Button></Card></div></main>}
